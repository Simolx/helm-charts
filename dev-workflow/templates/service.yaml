apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-workflow.fullname" . }}
  labels:
    {{- include "dev-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-workflow.selectorLabels" . | nindent 4 }}
